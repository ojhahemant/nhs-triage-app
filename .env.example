# OpenAI Configuration
# Get your API key from: https://platform.openai.com/api-keys
REACT_APP_OPENAI_API_KEY=your_openai_api_key_here

# OpenAI Model Configuration - using gpt-4o for both text and vision capabilities
REACT_APP_OPENAI_MODEL=gpt-4o
REACT_APP_OPENAI_TEMPERATURE=0.7

# Note: gpt-4o is a multimodal model that supports both text and images
# Alternative models: gpt-4o-mini (faster/cheaper), gpt-4-turbo (text-only)
# Note: In production, API calls should go through your backend server
# for security reasons. The browser implementation is for development only.
